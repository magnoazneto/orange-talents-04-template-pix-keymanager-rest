micronaut:
  application:
    name: restkeymanager

grpc:
  client:
    plaintext: true
    max-retry-attemps: 10
  channels:
    keyManager:
      address: 'localhost:50051'
      plaintext: true
      max-retry-attemps: 10
